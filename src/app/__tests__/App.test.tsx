// import { MemoryRouter } from 'react-router-dom';

describe('App', () => {
    it('should run without crashing', () => {
        // const { container } = render(
        //     <MemoryRouter>
        //         <App />
        //     </MemoryRouter>
        // );
        expect(1).toBe(1);
    });
});
